# ── Secrets / local env — NEVER commit ──
.env
.env.*
**/.env
**/.env.*
# Template with placeholder values is safe to commit
!.env.example

# ── OS / editor cruft ──
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# ── Build / temp ──
node_modules/
*.log
.playwright-mcp/
